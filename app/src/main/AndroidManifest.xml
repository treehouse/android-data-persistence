<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamtreehouse.mememaker" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MemeMakerApplication"
        android:allowBackup="true"
        android:icon="@drawable/treehouse_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MemeSettingsActivity" android:label="Settings" />
        <activity
            android:name=".ui.activities.CreateMemeActivity"
            android:label="@string/title_activity_create_meme"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>
